services:
  postgres:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_DB: darukaa_earth
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - darukaa-postgres:/var/lib/postgresql/data

volumes:
  darukaa-postgres: